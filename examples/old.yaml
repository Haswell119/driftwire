openapi: 3.0.0
info:
  title: Old
  version: 1.0.0
paths:
  /users:
    get:
      responses:
        "200":
          description: ok
          content:
            application/json:
              schema:
                type: object
                required: [id]
                properties:
                  id:
                    type: integer
                  name:
                    type: string
                  status:
                    type: string
                    enum: [active, inactive]
  /legacy:
    get:
      responses:
        "200":
          description: ok
